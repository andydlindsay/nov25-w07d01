const incrementCounter = require('./counter-fn');

incrementCounter();
incrementCounter();
incrementCounter();
incrementCounter();
incrementCounter();
console.log(incrementCounter());

// const Counter = require('./counter-class');

// const counter = new Counter();

// counter.incrementCounter();
// counter.incrementCounter();
// counter.incrementCounter();
// counter.incrementCounter();
// counter.incrementCounter();
// console.log(counter.incrementCounter());
